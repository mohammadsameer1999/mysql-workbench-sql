# SQL SELECT Statement – Notes with Examples

---

## 1️⃣ SELECT *
### Meaning
- `SELECT *` ka matlab hota hai **sabhi columns (all data)** retrieve karna
- `*` = **all columns**

### Example
```sql
SELECT * FROM users;

